# Personal Access Token used to authenticate GitHub API calls.
# Required for the GitHub Code Search endpoint and increases REST rate limits.
# Create at: https://github.com/settings/tokens (no scopes needed for public data).
GITHUB_TOKEN=

# Optional: override the path to the YAML config file.
# CONFIG_PATH=config.yaml
